#pragma once //make sure compile once

#include <iostream>
#include <string>

using namespace std;

namespace ariel{
    string snowman(long);
    string hat(int);
    string nose(int);
    string Leye(int);
    string Reye(int);
    string Lhand(int);
    string Rhand(int);
    string torso(int);
    string base(int);
}